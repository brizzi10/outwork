<h1><%= @category.name.upcase %></h1>
<p>
  <%= link_to "Edit/Delete", edit_category_path %>
</p>
<div class = "center">
</div>

<% @category.drills.each do |drill| %>
  <%= link_to image_tag(drill.image_url, border: 1), category_drill_path(@category, drill) %>
<%end%>

<%= link_to "+", new_category_drill_path(@category) %>
